import math
import re
from num_class import Num
from sym_class import Sym

class Row:
    def __init__(self, t):
        cells = t
        cooked = copy (t)
        isevaled = False
        
        `
    
