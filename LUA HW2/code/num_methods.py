#num methods
import random

def add(int v):
	if v!="?":
		self.n = self.n+1
		self.lo = min(v, self.lo)
		self.hi = max(v, self.hi)
		if len(self._has) < the.nums: # import the from CLI
			self.pos = 1 + len(self._has)
		elif random.random() < the.nums/self.n:
			self.pos = random.randint(0,len(self._has))
		if pos:
			self.isSorted = false
			self._has[pos] = int(v)

def deviation:
    a = sort(arr)
    return (per (arr,0.9)-per (arr,0,.1))/2.58

def median:
    return per (sort(arr), 0.5)
    
